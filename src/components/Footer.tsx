/* Bottom footer with contact info or links */

import React from "react";

const Footer: React.FC = () => {
    return (
        <footer>
            <p>© {new Date().getFullYear()} Akilisyou. All rights reserved.</p>
        </footer>
    );
};

export default Footer;
